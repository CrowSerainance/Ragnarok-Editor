<Window x:Class="ROMapOverlayEditor.MapSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Map to Import"
        Width="400" Height="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Select a map to import NPCs from:" FontWeight="Bold" Margin="0,0,0,8"/>

        <TextBox Grid.Row="1" x:Name="SearchBox" Margin="0,0,0,8" Padding="4"
                 TextChanged="SearchBox_TextChanged" ToolTip="Search maps..."/>

        <ListBox Grid.Row="2" x:Name="MapListBox" SelectionMode="Single"
                 MouseDoubleClick="MapListBox_MouseDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" Padding="4,2" FontFamily="Consolas"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Import Selected" Width="120" Padding="8,4" IsDefault="True" Click="ImportButton_Click"/>
            <Button Content="Cancel" Width="80" Padding="8,4" Margin="8,0,0,0" IsCancel="True" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
