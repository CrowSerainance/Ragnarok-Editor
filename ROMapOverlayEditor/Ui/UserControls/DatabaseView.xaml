<UserControl x:Class="ROMapOverlayEditor.UserControls.DatabaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="250"/>
        </Grid.ColumnDefinitions>
        
        <!-- List -->
        <DockPanel Grid.Column="0" Margin="4">
            <TextBlock Text="Mob Database (Stub)" DockPanel.Dock="Top" FontWeight="Bold" Margin="4"/>
            <ListBox x:Name="ResultsList" SelectionChanged="ResultsList_SelectionChanged">
               <ListBox.ItemTemplate>
                   <DataTemplate>
                       <StackPanel Orientation="Horizontal">
                           <TextBlock Text="{Binding Id}" Width="50" FontWeight="Bold"/>
                           <TextBlock Text="{Binding Name}"/>
                       </StackPanel>
                   </DataTemplate>
               </ListBox.ItemTemplate>
            </ListBox>
        </DockPanel>
        
        <!-- Details -->
        <Border Grid.Column="1" Margin="4" BorderBrush="#ccc" BorderThickness="1" Padding="8" x:Name="DetailsPanel" Visibility="Collapsed">
            <StackPanel>
                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16"/>
                <TextBlock Text="{Binding Id}" FontStyle="Italic"/>
                <Separator Margin="0,8"/>
                <TextBlock Text="{Binding Race}"/>
                <TextBlock Text="{Binding Element}"/>
                <TextBlock Text="{Binding Size}"/>
                <TextBlock Text="{Binding Hp, StringFormat='HP: {0}'}"/>
                
                <Button x:Name="PlaceButton" Content="PLACE SPAWN" Click="PlaceButton_Click" Margin="0,16,0,0" Height="30" Background="#dfd"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
