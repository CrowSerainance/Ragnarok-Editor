<Window x:Class="RoDbEditor.SetupWizard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RoDbEditor — Setup Wizard" Width="560" Height="480"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip"
        MinWidth="480" MinHeight="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="StepTitle" Grid.Row="0" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,16">
            <StackPanel x:Name="StepContent"/>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="BackButton" Content="Back" Width="80" Padding="12,6" Margin="0,0,8,0" Click="BackButton_Click" IsEnabled="False"/>
            <Button x:Name="NextButton" Content="Next" Width="80" Padding="12,6" Margin="0,0,8,0" Click="NextButton_Click"/>
            <Button x:Name="FinishButton" Content="Finish" Width="80" Padding="12,6" Visibility="Collapsed" Click="FinishButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
